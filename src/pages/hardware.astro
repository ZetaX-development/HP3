---
---
<!DOCTYPE html>
<html lang="ja" data-lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="icon" type="image/x-icon" href="/favicon.ico" />
  <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32x32.png" />
  <link rel="icon" type="image/png" sizes="16x16" href="/favicon-16x16.png" />
  <link rel="apple-touch-icon" href="/apple-touch-icon.png" />
  <title>ハードウェア開発 — ZetaX</title>
  <meta name="description" content="ZetaXのハードウェア開発。PCB基板設計から組み込みソフトウェアまで完全内製。製造業向け予兆保全デバイスはクラウド・WiFi不要で即日導入可能。3工場導入実績。">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Noto+Sans+JP:wght@200;300;400;500;700&family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
  <style is:global>
    *, *::before, *::after { margin: 0; padding: 0; box-sizing: border-box; }
    :root {
      --bg: #ffffff; --bg-subtle: #f7f8fa; --bg-card: #f9fafb;
      --text-primary: #0d0d14; --text-secondary: #52606d; --text-dim: #9da5b4;
      --accent: #1a3fbf; --accent-light: #3b5fdf; --accent-pale: #eef1fd;
      --border: #e4e7ef; --border-light: #f0f2f6;
      --font-jp: 'Noto Sans JP', sans-serif; --font-en: 'Inter', 'Noto Sans JP', sans-serif;
      --ease: cubic-bezier(0.22,1,0.36,1);
    }
    html { scroll-behavior: smooth; font-size: 16px; overflow-x: hidden; }
    body { font-family: var(--font-jp); background: var(--bg); color: var(--text-primary); line-height: 1.8; -webkit-font-smoothing: antialiased; }
    a { color: inherit; text-decoration: none; }
    .nav { position: fixed; top: 0; left: 0; right: 0; z-index: 1000; padding: 0 3rem; height: 68px; display: flex; justify-content: space-between; align-items: center; background: rgba(255,255,255,0.97); backdrop-filter: blur(16px); border-bottom: 1px solid var(--border); transition: box-shadow 0.3s; }
    .nav.elevated { box-shadow: 0 2px 12px rgba(0,0,0,0.06); }
    .nav-logo { font-family: var(--font-en); font-size: 1.15rem; font-weight: 700; letter-spacing: 0.12em; }
    .nav-logo span { color: var(--accent); }
    .nav-right { display: flex; align-items: center; }
    .nav-links { display: flex; list-style: none; font-size: 0.82rem; font-weight: 500; }
    .nav-links > li { position: relative; }
    .nav-links > li > a, .nav-links > li > .nav-dropdown-trigger { display: flex; align-items: center; gap: 4px; height: 68px; padding: 0 1.1rem; color: var(--text-secondary); font-size: 0.82rem; font-weight: 500; border: none; background: none; cursor: pointer; transition: color 0.2s; white-space: nowrap; }
    .nav-links > li > a:hover, .nav-links > li > .nav-dropdown-trigger:hover { color: var(--text-primary); }
    .nav-dropdown-arrow { font-size: 0.55rem; opacity: 0.5; transition: transform 0.2s; }
    .nav-links > li:hover .nav-dropdown-arrow { transform: rotate(180deg); }
    .nav-dropdown { position: absolute; top: calc(100% + 2px); left: 50%; transform: translateX(-50%) translateY(-6px); background: #fff; border: 1px solid var(--border); border-radius: 8px; box-shadow: 0 8px 32px rgba(0,0,0,0.08); padding: 0.6rem 0; min-width: 200px; opacity: 0; visibility: hidden; pointer-events: none; transition: opacity 0.18s var(--ease), transform 0.18s var(--ease); }
    .nav-links > li:hover .nav-dropdown { opacity: 1; visibility: visible; pointer-events: auto; transform: translateX(-50%) translateY(0); }
    .nav-dropdown a { display: block; padding: 0.6rem 1.4rem; font-size: 0.82rem; color: var(--text-secondary); transition: background 0.15s, color 0.15s; white-space: nowrap; }
    .nav-dropdown a:hover { background: var(--bg-subtle); color: var(--accent); }
    .nav-dropdown-divider { height: 1px; background: var(--border-light); margin: 0.4rem 0; }
    .nav-dropdown-label { padding: 0.5rem 1.4rem 0.2rem; font-family: var(--font-en); font-size: 0.6rem; font-weight: 700; letter-spacing: 0.18em; text-transform: uppercase; color: var(--text-dim); }
    .lang-toggle { display: flex; align-items: center; background: var(--bg-card); border-radius: 20px; padding: 3px; margin-left: 1.4rem; border: 1px solid var(--border); }
    .lang-btn { font-family: var(--font-en); font-size: 0.72rem; font-weight: 600; letter-spacing: 0.08em; padding: 5px 13px; border-radius: 16px; border: none; cursor: pointer; background: transparent; color: var(--text-dim); transition: all 0.2s; }
    .lang-btn.active { background: var(--accent); color: #fff; }
    .nav-cta { font-family: var(--font-jp); font-size: 0.82rem; font-weight: 600; padding: 9px 22px; border-radius: 6px; background: var(--accent); color: #fff; margin-left: 1.2rem; transition: background 0.2s, transform 0.2s; white-space: nowrap; }
    .nav-cta:hover { background: var(--accent-light); transform: translateY(-1px); }
    @media (max-width: 768px) { .nav-links, .nav-cta, .lang-toggle { display: none; } .nav { padding: 0 1.5rem; } }

    .container { max-width: 1160px; margin: 0 auto; padding: 0 3rem; }
    .section { padding: 100px 0; }
    .section-alt { background: var(--bg-subtle); }
    .section-label { display: inline-flex; align-items: center; gap: 0.6rem; font-family: var(--font-en); font-size: 0.66rem; font-weight: 700; letter-spacing: 0.25em; text-transform: uppercase; color: var(--accent); margin-bottom: 1.2rem; }
    .section-label::before { content: ''; width: 5px; height: 5px; border-radius: 50%; background: var(--accent); }
    .section-heading { font-family: var(--font-jp); font-size: clamp(1.5rem, 2.6vw, 2.2rem); font-weight: 700; line-height: 1.3; color: var(--text-primary); }
    .section-sub { font-size: 0.95rem; color: var(--text-secondary); line-height: 2; margin-top: 1rem; max-width: 640px; }
    .reveal { opacity: 0; transform: translateY(22px); transition: opacity 0.7s var(--ease), transform 0.7s var(--ease); }
    .reveal.visible { opacity: 1; transform: translateY(0); }
    .d1 { transition-delay: 0.1s; } .d2 { transition-delay: 0.2s; } .d3 { transition-delay: 0.3s; }
    @media (max-width: 1024px) { .container { padding: 0 2rem; } }
    @media (max-width: 768px) { .section { padding: 70px 0; } }

    /* HERO */
    .hero { min-height: 80vh; display: flex; align-items: center; background: var(--text-primary); padding-top: 68px; position: relative; overflow: hidden; }
    .hero-grid-bg { position: absolute; inset: 0; background-image: linear-gradient(to right, rgba(26,63,191,0.1) 1px, transparent 1px), linear-gradient(to bottom, rgba(26,63,191,0.1) 1px, transparent 1px); background-size: 44px 44px; opacity: 0.6; }
    .hero-accent { position: absolute; width: 500px; height: 500px; border-radius: 50%; background: radial-gradient(circle, rgba(26,63,191,0.25) 0%, transparent 70%); top: -150px; right: -50px; pointer-events: none; }
    .hero-content { position: relative; z-index: 1; padding: 5rem 0; }
    .hero-badge { display: inline-flex; align-items: center; gap: 0.5rem; font-family: var(--font-en); font-size: 0.66rem; font-weight: 700; letter-spacing: 0.25em; text-transform: uppercase; color: #7b96ff; margin-bottom: 2rem; }
    .hero-badge::before { content: ''; width: 5px; height: 5px; border-radius: 50%; background: #7b96ff; }
    .hero-title { font-family: var(--font-jp); font-size: clamp(2rem, 4vw, 3.4rem); font-weight: 700; line-height: 1.25; color: #fff; margin-bottom: 1.6rem; }
    .hero-title .accent { color: #7b96ff; }
    .hero-sub { font-size: clamp(0.9rem, 1.4vw, 1.05rem); color: rgba(255,255,255,0.52); line-height: 1.9; max-width: 580px; margin-bottom: 2.8rem; }
    .hero-specs { display: flex; gap: 1px; background: rgba(255,255,255,0.08); border: 1px solid rgba(255,255,255,0.08); margin-bottom: 2.8rem; flex-wrap: wrap; }
    .hero-spec { padding: 1.2rem 2rem; flex: 1; min-width: 160px; }
    .hero-spec-key { font-family: var(--font-en); font-size: 0.58rem; font-weight: 700; letter-spacing: 0.2em; text-transform: uppercase; color: #7b96ff; margin-bottom: 0.4rem; }
    .hero-spec-val { font-size: 0.9rem; font-weight: 600; color: #fff; line-height: 1.4; }
    .hero-actions { display: flex; gap: 1rem; flex-wrap: wrap; }
    .btn-primary { font-family: var(--font-jp); font-size: 0.9rem; font-weight: 600; padding: 13px 34px; border-radius: 6px; background: #fff; color: var(--text-primary); border: none; cursor: pointer; display: inline-block; transition: background 0.2s, transform 0.2s; }
    .btn-primary:hover { background: rgba(255,255,255,0.9); transform: translateY(-2px); }
    .btn-outline { font-family: var(--font-jp); font-size: 0.9rem; font-weight: 500; padding: 13px 34px; border-radius: 6px; background: transparent; color: rgba(255,255,255,0.65); border: 1.5px solid rgba(255,255,255,0.22); cursor: pointer; display: inline-block; transition: border-color 0.2s, color 0.2s, transform 0.2s; }
    .btn-outline:hover { border-color: rgba(255,255,255,0.55); color: #fff; transform: translateY(-2px); }

    .breadcrumb { padding: 1.4rem 0; border-bottom: 1px solid var(--border); }
    .breadcrumb-inner { display: flex; align-items: center; gap: 0.6rem; font-family: var(--font-en); font-size: 0.72rem; color: var(--text-dim); }
    .breadcrumb-inner a { color: var(--text-dim); transition: color 0.2s; }
    .breadcrumb-inner a:hover { color: var(--accent); }

    /* PRODUCT HIGHLIGHT */
    .product-hl { background: var(--text-primary); padding: 80px 0; }
    .product-hl-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 5rem; align-items: start; }
    .hl-label { font-family: var(--font-en); font-size: 0.65rem; font-weight: 700; letter-spacing: 0.25em; text-transform: uppercase; color: #7b96ff; margin-bottom: 1.2rem; display: flex; align-items: center; gap: 0.5rem; }
    .hl-label::before { content: ''; width: 5px; height: 5px; border-radius: 50%; background: #7b96ff; }
    .hl-heading { font-family: var(--font-jp); font-size: clamp(1.3rem, 2.2vw, 1.9rem); font-weight: 700; color: #fff; line-height: 1.35; margin-bottom: 1.2rem; }
    .hl-desc { font-size: 0.9rem; color: rgba(255,255,255,0.52); line-height: 2; }
    .hl-badge { display: inline-flex; align-items: center; gap: 0.4rem; font-size: 0.75rem; font-weight: 600; padding: 5px 14px; border-radius: 2px; background: rgba(123,150,255,0.15); color: #7b96ff; margin-top: 1.4rem; }
    .hl-badge::before { content: ''; width: 5px; height: 5px; border-radius: 50%; background: #7b96ff; }
    .spec-table { width: 100%; border-collapse: collapse; }
    .spec-table tr { border-bottom: 1px solid rgba(255,255,255,0.07); }
    .spec-table th { padding: 1rem 0; text-align: left; font-size: 0.78rem; font-weight: 600; color: rgba(255,255,255,0.42); width: 44%; vertical-align: top; }
    .spec-table td { padding: 1rem 1rem; font-size: 0.84rem; color: rgba(255,255,255,0.72); line-height: 1.6; }
    @media (max-width: 1024px) { .product-hl-grid { grid-template-columns: 1fr; gap: 3rem; } }

    /* CAPABILITIES */
    .cap-grid { display: grid; grid-template-columns: repeat(3,1fr); gap: 1px; background: var(--border); border: 1px solid var(--border); margin-top: 4rem; }
    .cap-card { background: var(--bg); padding: 2.6rem 2.2rem; transition: background 0.2s; }
    .cap-card:hover { background: #fafbff; }
    .cap-num { font-family: var(--font-en); font-size: 0.62rem; font-weight: 700; letter-spacing: 0.2em; color: var(--accent); margin-bottom: 1rem; }
    .cap-title { font-weight: 700; font-size: 0.96rem; color: var(--text-primary); margin-bottom: 0.7rem; line-height: 1.5; }
    .cap-desc { font-size: 0.84rem; color: var(--text-secondary); line-height: 1.9; }
    @media (max-width: 768px) { .cap-grid { grid-template-columns: 1fr; } }

    /* STRENGTH — dark strip */
    .strength-strip { background: var(--text-primary); padding: 60px 0; }
    .strength-grid { display: grid; grid-template-columns: repeat(4,1fr); gap: 1px; background: rgba(255,255,255,0.06); border: 1px solid rgba(255,255,255,0.07); }
    .strength-item { background: rgba(255,255,255,0.02); padding: 2rem 1.8rem; }
    .strength-num { font-family: var(--font-en); font-size: 1.6rem; font-weight: 700; color: #fff; line-height: 1; margin-bottom: 0.3rem; }
    .strength-num em { font-style: normal; color: #7b96ff; font-size: 0.6em; }
    .strength-label { font-size: 0.76rem; color: rgba(255,255,255,0.38); line-height: 1.5; }
    @media (max-width: 768px) { .strength-grid { grid-template-columns: repeat(2,1fr); } }

    /* FLOW */
    .flow-grid { display: grid; grid-template-columns: repeat(4,1fr); gap: 1px; background: var(--border); border: 1px solid var(--border); margin-top: 4rem; }
    .flow-step { background: var(--bg); padding: 2.4rem 2rem; }
    .flow-num { font-family: var(--font-en); font-size: 0.6rem; font-weight: 700; letter-spacing: 0.2em; color: var(--accent); margin-bottom: 0.8rem; }
    .flow-title { font-weight: 700; font-size: 0.92rem; color: var(--text-primary); margin-bottom: 0.6rem; }
    .flow-desc { font-size: 0.8rem; color: var(--text-secondary); line-height: 1.9; }
    @media (max-width: 768px) { .flow-grid { grid-template-columns: repeat(2,1fr); } }

    /* FAQ */
    .faq-list { margin-top: 3rem; border-top: 1px solid var(--border); }
    .faq-item { border-bottom: 1px solid var(--border); }
    .faq-q { width: 100%; text-align: left; padding: 1.6rem 0; display: flex; justify-content: space-between; align-items: center; background: none; border: none; cursor: pointer; font-family: var(--font-jp); font-size: 0.96rem; font-weight: 600; color: var(--text-primary); gap: 1rem; }
    .faq-icon { width: 22px; height: 22px; flex-shrink: 0; border: 1.5px solid var(--border); border-radius: 50%; display: flex; align-items: center; justify-content: center; font-size: 0.75rem; color: var(--text-dim); transition: transform 0.2s, background 0.2s, color 0.2s; }
    .faq-item.open .faq-icon { background: var(--accent); border-color: var(--accent); color: #fff; transform: rotate(45deg); }
    .faq-a { display: none; padding: 0 0 1.6rem; font-size: 0.88rem; color: var(--text-secondary); line-height: 2; }
    .faq-item.open .faq-a { display: block; }

    /* CTA */
    .cta-section { background: var(--text-primary); padding: 80px 0; position: relative; overflow: hidden; }
    .cta-mesh { position: absolute; inset: 0; background: radial-gradient(ellipse 70% 80% at 15% 50%, rgba(26,63,191,0.3) 0%, transparent 60%), radial-gradient(ellipse 50% 70% at 85% 40%, rgba(123,150,255,0.2) 0%, transparent 55%); pointer-events: none; }
    .cta-inner { position: relative; z-index: 1; text-align: center; }
    .cta-label { font-family: var(--font-en); font-size: 0.66rem; font-weight: 700; letter-spacing: 0.25em; text-transform: uppercase; color: #7b96ff; margin-bottom: 1.4rem; display: flex; align-items: center; justify-content: center; gap: 0.6rem; }
    .cta-label::before { content: ''; width: 5px; height: 5px; border-radius: 50%; background: #7b96ff; }
    .cta-title { font-family: var(--font-jp); font-size: clamp(1.5rem, 2.6vw, 2.2rem); font-weight: 700; color: #fff; line-height: 1.35; margin-bottom: 1.2rem; }
    .cta-sub { font-size: 0.92rem; color: rgba(255,255,255,0.5); line-height: 1.9; margin-bottom: 2.8rem; }
    .cta-btn { font-family: var(--font-jp); font-size: 0.96rem; font-weight: 700; padding: 15px 44px; border-radius: 6px; background: #fff; color: var(--text-primary); border: none; cursor: pointer; display: inline-block; transition: background 0.2s, transform 0.2s; }
    .cta-btn:hover { background: rgba(255,255,255,0.9); transform: translateY(-2px); }

    .footer { background: var(--text-primary); padding: 48px 0 32px; border-top: 1px solid rgba(255,255,255,0.05); }
    .footer-inner { display: flex; justify-content: space-between; align-items: center; flex-wrap: wrap; gap: 1.5rem; }
    .footer-logo { font-family: var(--font-en); font-size: 1.1rem; font-weight: 700; letter-spacing: 0.1em; color: #fff; }
    .footer-logo span { color: #7b96ff; }
    .footer-nav { display: flex; gap: 1.8rem; flex-wrap: wrap; }
    .footer-nav a { font-size: 0.8rem; color: rgba(255,255,255,0.38); transition: color 0.2s; }
    .footer-nav a:hover { color: rgba(255,255,255,0.7); }
    .footer-copy { font-family: var(--font-en); font-size: 0.72rem; color: rgba(255,255,255,0.2); margin-top: 2rem; padding-top: 1.5rem; border-top: 1px solid rgba(255,255,255,0.06); }
  </style>
</head>
<body>

  <nav class="nav" id="nav">
    <a href="/" class="nav-logo">ZETA<span>X</span></a>
    <div class="nav-right">
      <ul class="nav-links">
        <li><a href="/#why">強み</a></li>
        <li>
          <button class="nav-dropdown-trigger">サービス <span class="nav-dropdown-arrow">▼</span></button>
          <div class="nav-dropdown">
            <p class="nav-dropdown-label">Services</p>
            <a href="/ai-dx">AI/ITコンサル・開発</a>
            <a href="/security">セキュリティコンサル</a>
            <a href="/ai-training">AI研修</a>
            <a href="/hardware">ハードウェア開発</a>
            <div class="nav-dropdown-divider"></div>
            <p class="nav-dropdown-label">Products</p>
            <a href="/#products">自社プロダクト</a>
          </div>
        </li>
        <li>
          <button class="nav-dropdown-trigger">実績 <span class="nav-dropdown-arrow">▼</span></button>
          <div class="nav-dropdown">
            <a href="/#cases">業種別支援事例</a>
            <a href="/#testimonials">お客様の声</a>
          </div>
        </li>
        <li>
          <button class="nav-dropdown-trigger">会社 <span class="nav-dropdown-arrow">▼</span></button>
          <div class="nav-dropdown">
            <a href="/about">会社紹介（詳細）</a>
            <a href="/#company">会社概要</a>
            <a href="/about#founders">代表紹介</a>
          </div>
        </li>
      </ul>
      <div class="lang-toggle">
        <button class="lang-btn active" data-lang="ja">JP</button>
        <button class="lang-btn" data-lang="en">EN</button>
      </div>
      <a href="/#contact" class="nav-cta">お問い合わせ</a>
    </div>
  </nav>

  <div class="breadcrumb" style="margin-top:68px;">
    <div class="container">
      <div class="breadcrumb-inner">
        <a href="/">Home</a><span>›</span>
        <a href="/#services">Services</a><span>›</span>
        <span>ハードウェア開発</span>
      </div>
    </div>
  </div>

  <!-- HERO -->
  <section class="hero">
    <div class="hero-grid-bg"></div>
    <div class="hero-accent"></div>
    <div class="container">
      <div class="hero-content">
        <p class="hero-badge">Service 04 — Hardware Development</p>
        <h1 class="hero-title">PCBから、<br>組み込みまで。<br><span class="accent">ハードウェアを、</span><br><span class="accent">完全内製で作る。</span></h1>
        <p class="hero-sub">基板設計から組み込みソフトウェアの実装まで、すべて自社で行います。クラウド・WiFi不要で動作する製造業向け予兆保全デバイスは、3工場への導入実績があります。</p>
        <div class="hero-specs">
          <div class="hero-spec">
            <div class="hero-spec-key">Flagship Product</div>
            <div class="hero-spec-val">製造業向け<br>予兆保全デバイス</div>
          </div>
          <div class="hero-spec">
            <div class="hero-spec-key">Connectivity</div>
            <div class="hero-spec-val">クラウド不要<br>WiFi不要</div>
          </div>
          <div class="hero-spec">
            <div class="hero-spec-key">Deployment</div>
            <div class="hero-spec-val">3工場<br>導入済み</div>
          </div>
          <div class="hero-spec">
            <div class="hero-spec-key">Development</div>
            <div class="hero-spec-val">PCB〜組み込みまで<br>完全内製</div>
          </div>
        </div>
        <div class="hero-actions">
          <a href="/#contact" class="btn-primary">ハードウェア開発を相談する（無料）</a>
          <a href="#product" class="btn-outline">予兆保全デバイスを見る</a>
        </div>
      </div>
    </div>
  </section>

  <!-- FLAGSHIP PRODUCT -->
  <div class="product-hl" id="product">
    <div class="container">
      <div class="product-hl-grid">
        <div class="reveal">
          <p class="hl-label">Flagship Product</p>
          <h2 class="hl-heading">製造業向け<br>予兆保全デバイス</h2>
          <p class="hl-desc">工場の機械が壊れる前に異常を検知し、突発的な設備停止を未然に防ぎます。振動・温度・電流などのセンサーデータをデバイス上でリアルタイム解析。クラウドやWiFiに依存しないため、インターネット環境が整っていない工場でもそのまま導入できます。</p>
          <p style="font-size:0.88rem;color:rgba(255,255,255,0.45);line-height:1.9;margin-top:1rem;">機械が止まってから修理するのではなく、止まる前に予兆を掴む。突発停止による生産ラインのダウンタイムを削減し、計画的なメンテナンスを可能にします。</p>
          <span class="hl-badge">3工場に導入済み</span>
        </div>
        <div class="reveal d1">
          <table class="spec-table">
            <tr>
              <th>検知方式</th>
              <td>振動・温度・電流センサーによるリアルタイム異常検知</td>
            </tr>
            <tr>
              <th>ネットワーク</th>
              <td>クラウド接続不要・WiFi不要・完全スタンドアロン動作</td>
            </tr>
            <tr>
              <th>導入方式</th>
              <td>既存設備への取り付け。電源接続のみで即日稼働可能</td>
            </tr>
            <tr>
              <th>アラート</th>
              <td>異常検知時に即時アラート。閾値・感度はカスタマイズ可</td>
            </tr>
            <tr>
              <th>データ管理</th>
              <td>デバイス内蔵ストレージに記録。定期的なレポート出力対応</td>
            </tr>
            <tr>
              <th>対応設備</th>
              <td>モーター・コンプレッサー・ポンプ・コンベアなど回転機械全般</td>
            </tr>
            <tr>
              <th>開発方式</th>
              <td>PCB基板設計〜組み込みファームウェアまで完全内製</td>
            </tr>
            <tr>
              <th>カスタマイズ</th>
              <td>センサー種類・検知ロジック・筐体設計をお客様仕様に変更可</td>
            </tr>
          </table>
        </div>
      </div>
    </div>
  </div>

  <!-- CAPABILITIES -->
  <section class="section" id="capabilities">
    <div class="container">
      <div class="reveal">
        <span class="section-label">Capabilities</span>
        <h2 class="section-heading">ハードウェア開発領域</h2>
        <p class="section-sub">PCBの設計から量産まで、ハードウェア開発のすべての工程を自社でカバーします。</p>
      </div>
      <div class="cap-grid">
        <div class="cap-card reveal d1">
          <div class="cap-num">01</div>
          <h3 class="cap-title">PCB基板設計・レイアウト</h3>
          <p class="cap-desc">回路設計からKiCad / Altiumを使用したPCBレイアウトまで対応します。電源設計・信号整合・EMC対策を考慮した基板設計を行います。プロトタイプ〜量産基板まで対応。</p>
        </div>
        <div class="cap-card reveal d2">
          <div class="cap-num">02</div>
          <h3 class="cap-title">組み込みファームウェア開発</h3>
          <p class="cap-desc">C/C++・MicroPythonによるファームウェア開発。ARM Cortex-M系・ESP32・STM32などのマイコンに対応。RTOS（FreeRTOS）の使用経験あり。センサードライバ・通信プロトコル実装まで担当します。</p>
        </div>
        <div class="cap-card reveal d3">
          <div class="cap-num">03</div>
          <h3 class="cap-title">センサー・IoTシステム設計</h3>
          <p class="cap-desc">振動・温度・電流・圧力・画像など各種センサーの選定・回路設計・信号処理実装を行います。スタンドアロン動作からLoRa・BLEなどの無線通信を使ったIoTシステムまで設計します。</p>
        </div>
        <div class="cap-card reveal d1">
          <div class="cap-num">04</div>
          <h3 class="cap-title">エッジAI・オンデバイス推論</h3>
          <p class="cap-desc">TensorFlow Lite・Edge Impulseを使ったエッジAIモデルの開発・最適化・デプロイ。クラウド不要でデバイス上でAI推論を実行します。異常検知・分類・予測などのモデルを実装します。</p>
        </div>
        <div class="cap-card reveal d2">
          <div class="cap-num">05</div>
          <h3 class="cap-title">筐体・メカ設計</h3>
          <p class="cap-desc">3D CADを使用した筐体設計。3Dプリンタでの試作から、射出成形・板金加工での量産筐体製作まで対応します。防塵・防水（IP規格）・耐振動設計も承ります。</p>
        </div>
        <div class="cap-card reveal d3">
          <div class="cap-num">06</div>
          <h3 class="cap-title">ソフトウェア連携・ダッシュボード</h3>
          <p class="cap-desc">ハードウェアから収集したデータを可視化するダッシュボード・管理システムの開発まで一貫して対応します。ハードとソフトを同じチームが担当するため、密な連携が可能です。</p>
        </div>
      </div>
    </div>
  </section>

  <!-- STRENGTH NUMBERS -->
  <div class="strength-strip">
    <div class="container">
      <div class="strength-grid reveal">
        <div class="strength-item">
          <div class="strength-num">3<em>工場</em></div>
          <div class="strength-label">予兆保全デバイス<br>導入実績</div>
        </div>
        <div class="strength-item">
          <div class="strength-num">0<em>社</em></div>
          <div class="strength-label">外部委託<br>（完全内製）</div>
        </div>
        <div class="strength-item">
          <div class="strength-num">PCB</div>
          <div class="strength-label">基板設計〜量産まで<br>一貫対応</div>
        </div>
        <div class="strength-item">
          <div class="strength-num">即日</div>
          <div class="strength-label">ネットワーク不要<br>現場に置くだけ</div>
        </div>
      </div>
    </div>
  </div>

  <!-- FLOW -->
  <section class="section section-alt" id="flow">
    <div class="container">
      <div class="reveal">
        <span class="section-label">Development Flow</span>
        <h2 class="section-heading">開発の進め方</h2>
        <p class="section-sub">PoC（概念実証）から量産まで、お客様の段階に合わせて柔軟に対応します。</p>
      </div>
      <div class="flow-grid">
        <div class="flow-step reveal d1">
          <div class="flow-num">PHASE 01</div>
          <div class="flow-title">要件定義・PoC設計</div>
          <div class="flow-desc">何を検知・制御したいのかをヒアリングし、実現方式を検討します。既存の機械・センサー・環境を確認した上で最適なアーキテクチャを提案します。</div>
        </div>
        <div class="flow-step reveal d2">
          <div class="flow-num">PHASE 02</div>
          <div class="flow-title">プロトタイプ制作</div>
          <div class="flow-desc">市販のマイコンボード・センサーモジュールを使って動作するプロトタイプを製作。実際の現場で動かして検証し、仕様を確定させます。</div>
        </div>
        <div class="flow-step reveal d3">
          <div class="flow-num">PHASE 03</div>
          <div class="flow-title">カスタム基板設計・開発</div>
          <div class="flow-desc">プロトタイプで確認した仕様をもとに、カスタムPCBを設計・発注します。ファームウェアと並行して開発し、現場環境でのテストを繰り返します。</div>
        </div>
        <div class="flow-step reveal d1">
          <div class="flow-num">PHASE 04</div>
          <div class="flow-title">現場導入・継続サポート</div>
          <div class="flow-desc">実際の工場・現場に赴いて設置・動作確認を行います。導入後も定期的なファームウェアアップデート・異常対応・機能追加に対応します。</div>
        </div>
      </div>
    </div>
  </section>

  <!-- FAQ -->
  <section class="section" id="faq">
    <div class="container">
      <div class="reveal">
        <span class="section-label">FAQ</span>
        <h2 class="section-heading">よくあるご質問</h2>
      </div>
      <div class="faq-list reveal d1">
        <div class="faq-item">
          <button class="faq-q">予兆保全デバイスはどんな機械に対応していますか？<span class="faq-icon">+</span></button>
          <div class="faq-a">モーター・コンプレッサー・ポンプ・コンベア・ファンなど、回転を伴う機械全般に対応しています。振動センサーを取り付けることで、異常振動・軸受の劣化・アンバランスなどの兆候を早期に検知します。特殊な設備への対応は、現地確認の上でご提案します。</div>
        </div>
        <div class="faq-item">
          <button class="faq-q">工場にインターネット環境がありませんが、導入できますか？<span class="faq-icon">+</span></button>
          <div class="faq-a">はい、それが最大の特徴です。ZetaXの予兆保全デバイスはクラウドへの接続・WiFiを一切必要としません。デバイス内でデータ処理とAI推論を完結させるため、ネットワークが届かない工場内・地下・屋外でも動作します。電源を接続するだけで即日稼働できます。</div>
        </div>
        <div class="faq-item">
          <button class="faq-q">既製品のIoTセンサーと何が違うのですか？<span class="faq-icon">+</span></button>
          <div class="faq-a">主な違いは3点です。①クラウド不要のスタンドアロン動作、②お客様の設備・ニーズに合わせたカスタム設計、③ZetaXが開発からサポートまで一貫して担当する点です。既製品は汎用的なため、特殊な設備・特殊な検知要件には対応できないケースがあります。</div>
        </div>
        <div class="faq-item">
          <button class="faq-q">センサーの種類や検知ロジックは変更できますか？<span class="faq-icon">+</span></button>
          <div class="faq-a">はい、完全にカスタマイズ可能です。センサーの種類・個数・配置、検知ロジック・閾値、アラートの方法（LED・ブザー・外部出力）などをお客様の要件に合わせて設計します。まずはヒアリングにてご要望をお聞かせください。</div>
        </div>
        <div class="faq-item">
          <button class="faq-q">ハードウェア開発とソフトウェア開発を同時に依頼できますか？<span class="faq-icon">+</span></button>
          <div class="faq-a">はい、ZetaXはハードウェア・ソフトウェア・AIすべてを自社で開発できます。例えば「予兆保全デバイス＋工場管理システム（クラウド）＋スマートフォンアプリ」のようなシステム全体を、一つのチームで担当できます。引き継ぎがないため、密なハードソフト協調設計が可能です。</div>
        </div>
      </div>
    </div>
  </section>

  <!-- CTA -->
  <section class="cta-section">
    <div class="cta-mesh"></div>
    <div class="container">
      <div class="cta-inner reveal">
        <p class="cta-label">Contact</p>
        <h2 class="cta-title">まずは現場を<br>見せてください。</h2>
        <p class="cta-sub">どんな機械で、何を検知したいか。<br>現地確認の上で最適な提案をします。初回は無料です。</p>
        <a href="/#contact" class="cta-btn">ハードウェア開発を相談する →</a>
      </div>
    </div>
  </section>

  <footer class="footer">
    <div class="container">
      <div class="footer-inner">
        <a href="/" class="footer-logo">ZETA<span>X</span></a>
        <nav class="footer-nav">
          <a href="/ai-dx">AI/ITコンサル・開発</a>
          <a href="/security">セキュリティコンサル</a>
          <a href="/ai-training">AI研修</a>
          <a href="/hardware">ハードウェア開発</a>
          <a href="/about">会社紹介</a>
          <a href="/#contact">お問い合わせ</a>
        </nav>
      </div>
      <div class="footer-copy">
        <div style="display:flex;justify-content:space-between;align-items:center;flex-wrap:wrap;gap:1rem;">
          <span>&copy; 2025 ZetaX Inc. All rights reserved.</span>
          <div style="display:flex;gap:1.5rem;">
            <a href="/security-policy" style="color:rgba(255,255,255,0.25)">Security Policy</a>
            <a href="/privacy-policy" style="color:rgba(255,255,255,0.25)">Privacy Policy</a>
            <a href="/terms" style="color:rgba(255,255,255,0.25)">Terms</a>
          </div>
        </div>
      </div>
    </div>
  </footer>

  <script>
    const nav = document.getElementById('nav');
    window.addEventListener('scroll', () => nav.classList.toggle('elevated', scrollY > 10), { passive: true });
    const obs = new IntersectionObserver(entries => entries.forEach(e => { if (e.isIntersecting) { e.target.classList.add('visible'); obs.unobserve(e.target); } }), { threshold: 0.07, rootMargin: '0px 0px -40px 0px' });
    document.querySelectorAll('.reveal').forEach(el => obs.observe(el));
    document.querySelectorAll('.faq-q').forEach(btn => {
      btn.addEventListener('click', () => {
        const item = btn.closest('.faq-item');
        const isOpen = item.classList.contains('open');
        document.querySelectorAll('.faq-item').forEach(i => i.classList.remove('open'));
        if (!isOpen) item.classList.add('open');
      });
    });
  </script>
</body>
</html>
