<!DOCTYPE html>
<html lang="ja" data-lang="ja">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>製造業向け 異常検知システム | エッジAIで予兆保全・振動監視 | ZetaX</title>
  <meta name="description" content="東大・東工大発の独自アルゴリズムによる製造業向け異常検知システム。GPU不要・クラウド不要のエッジAIで、振動・温度・電流のリアルタイム監視と予兆保全を実現。導入初日から検知開始、低消費電力・少データ学習で町工場からプライム上場企業まで対応。">
  <meta name="keywords" content="異常検知,製造業,予兆保全,エッジAI,GPU不要,クラウド不要,振動解析,予知保全,IoT,ディープテック,東京大学,スマートファクトリー,設備保全,ZetaX">
  <meta name="author" content="ZetaX, Inc.">
  <meta name="robots" content="index,follow,max-image-preview:large,max-snippet:-1,max-video-preview:-1">
  <link rel="canonical" href="https://zetax.jp/anomaly-detection">
  <meta name="format-detection" content="telephone=no">
  <!-- Open Graph -->
  <meta property="og:type" content="website">
  <meta property="og:site_name" content="ZetaX">
  <meta property="og:title" content="製造業向け 異常検知システム | エッジAIで予兆保全 | ZetaX">
  <meta property="og:description" content="東大・東工大発の独自アルゴリズムによる製造業向け異常検知システム。GPU不要・クラウド不要のエッジAIで、振動・温度・電流のリアルタイム監視と予兆保全を実現。">
  <meta property="og:url" content="https://zetax.jp/anomaly-detection">
  <meta property="og:locale" content="ja_JP">
  <meta property="og:locale:alternate" content="en_US">
  <!-- Twitter -->
  <meta name="twitter:card" content="summary_large_image">
  <meta name="twitter:title" content="製造業向け 異常検知システム | ZetaX">
  <meta name="twitter:description" content="東大・東工大発の独自アルゴリズムによる製造業向け異常検知システム。GPU不要・クラウド不要のエッジAI。">
  <!-- JSON-LD: Product / Service -->
  <script type="application/ld+json">
  {
    "@context": "https://schema.org",
    "@type": "Service",
    "serviceType": "Manufacturing Anomaly Detection",
    "name": "製造業向け 異常検知システム",
    "description": "東大・東工大発の独自アルゴリズムによる製造業向け異常検知システム。GPU不要・クラウド不要のエッジAIで、振動・温度・電流のリアルタイム監視と予兆保全を実現。",
    "provider": {
      "@type": "Organization",
      "name": "ZetaX, Inc.",
      "url": "https://zetax.jp",
      "logo": "https://zetax.jp/logo.png",
      "email": "contact@zetax.jp",
      "address": {
        "@type": "PostalAddress",
        "streetAddress": "神南1-11-4 FPGリンクス神南5階",
        "addressLocality": "渋谷区",
        "addressRegion": "東京都",
        "postalCode": "150-0041",
        "addressCountry": "JP"
      }
    },
    "areaServed": "JP",
    "audience": { "@type": "BusinessAudience", "audienceType": "Manufacturing" },
    "offers": {
      "@type": "Offer",
      "availability": "https://schema.org/InStock",
      "priceCurrency": "JPY"
    }
  }
  </script>
  <script type="application/ld+json">
  {
    "@context": "https://schema.org",
    "@type": "BreadcrumbList",
    "itemListElement": [
      { "@type": "ListItem", "position": 1, "name": "Home", "item": "https://zetax.jp/" },
      { "@type": "ListItem", "position": 2, "name": "製造業向け 異常検知システム", "item": "https://zetax.jp/anomaly-detection" }
    ]
  }
  </script>
  <script type="application/ld+json">
  {
    "@context": "https://schema.org",
    "@type": "FAQPage",
    "mainEntity": [
      {
        "@type": "Question",
        "name": "異常検知システムはクラウド接続が必要ですか？",
        "acceptedAnswer": { "@type": "Answer", "text": "いいえ。すべてエッジデバイス上で完結するため、ネットワーク環境のない現場にも導入可能です。製造データを外部に出さないためセキュリティリスクもありません。" }
      },
      {
        "@type": "Question",
        "name": "導入までにどれくらいの学習データが必要ですか？",
        "acceptedAnswer": { "@type": "Answer", "text": "独自アルゴリズムは少量の正常稼働データから短時間でモデルを構築できるため、数年分のデータがない現場でも導入初日から検知を開始できます。" }
      },
      {
        "@type": "Question",
        "name": "どのような設備に対応していますか？",
        "acceptedAnswer": { "@type": "Answer", "text": "回転機器（モーター・ポンプ・コンプレッサー）、配管・バルブ、コンベヤ・搬送機器、空調・環境設備、集塵機・排気設備、外観検査など、幅広い設備領域に対応します。" }
      },
      {
        "@type": "Question",
        "name": "GPU は必要ですか？",
        "acceptedAnswer": { "@type": "Answer", "text": "GPU は不要です。独自アルゴリズムは極めて低消費電力で動作するため、軽量なエッジデバイスのみで運用できます。" }
      }
    ]
  }
  </script>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link
    href="https://fonts.googleapis.com/css2?family=Noto+Sans+JP:wght@200;300;400;500;700&family=Space+Grotesk:wght@400;500;600;700&display=swap"
    rel="stylesheet">
  <style is:global>
    *, *::before, *::after { margin: 0; padding: 0; box-sizing: border-box; }
    :root {
      --bg: #ffffff;
      --bg-subtle: #f5f5f7;
      --bg-card: #f8f8f8;
      --text-primary: #111111;
      --text-secondary: #555555;
      --text-dim: #999999;
      --accent: #3B82F6;
      --accent-bright: #2563EB;
      --accent-glow: rgba(59,130,246,0.08);
      --accent-cyan: #0891B2;
      --silver: #888888;
      --font-jp: 'Noto Sans JP', sans-serif;
      --font-en: 'Noto Sans JP', sans-serif;
      --font-display: 'Noto Sans JP', sans-serif;
      --transition: cubic-bezier(0.22, 1, 0.36, 1);
    }
    html { scroll-behavior: smooth; font-size: 16px; background: var(--bg); overflow-x: hidden; }
    body { font-family: var(--font-jp); background: var(--bg); color: var(--text-primary); line-height: 1.8; -webkit-font-smoothing: antialiased; overflow-x: hidden; }
    a { color: inherit; text-decoration: none; }

    .nav-actions { display: flex; align-items: center; gap: 1rem; }
    .nav-cta { display: inline-flex; align-items: center; font-family: var(--font-jp); font-size: 0.8rem; font-weight: 700; letter-spacing: 0.04em; padding: 0.6rem 1.2rem; background: linear-gradient(135deg, #0e7490 0%, #0891b2 50%, #06b6d4 100%); color: #ffffff !important; border-radius: 8px; box-shadow: 0 4px 14px rgba(8,145,178,0.3); transition: transform 0.2s var(--transition), box-shadow 0.2s var(--transition); white-space: nowrap; }
    .nav-cta:hover { transform: translateY(-1px); box-shadow: 0 8px 22px rgba(8,145,178,0.4); }
    .lang-toggle { display: flex; align-items: center; gap: 0; background: rgba(0,0,0,0.04); border-radius: 20px; padding: 3px; margin-left: 0; border: 1px solid rgba(0,0,0,0.08); }
    .lang-btn { font-family: var(--font-en); font-size: 0.75rem; font-weight: 600; letter-spacing: 0.08em; padding: 5px 14px; border-radius: 16px; border: none; cursor: pointer; background: transparent; color: var(--text-dim); transition: all 0.3s ease; }
    .lang-btn.active { background: var(--accent); color: #fff; box-shadow: 0 2px 8px rgba(59,130,246,0.3); }
    .lang-btn:hover:not(.active) { color: #111111; }

    .nav { position: fixed; top: 0; left: 0; right: 0; z-index: 100; display: flex; align-items: center; justify-content: space-between; padding: 1.2rem 3rem; background: rgba(255,255,255,0.97); backdrop-filter: blur(12px); border-bottom: 1px solid rgba(0,0,0,0.06); transition: box-shadow 0.3s; }
    .nav.scrolled { box-shadow: 0 2px 12px rgba(0,0,0,0.08); }
    .nav-logo { font-family: var(--font-en); font-size: 1.3rem; font-weight: 700; letter-spacing: 0.12em; color: #111111; }
    .nav-logo span { color: var(--accent); }
    .nav-links { display: flex; gap: 2rem; list-style: none; }
    .nav-links a { font-family: var(--font-en); font-size: 0.82rem; font-weight: 500; letter-spacing: 0.06em; color: #666666; transition: color 0.2s; }
    .nav-links a:hover { color: #111111; }

    .page-hero { padding: 11rem 2rem 5rem; text-align: center; max-width: 760px; margin: 0 auto; }
    @media (max-width: 768px) { .page-hero { padding: 9rem 1.5rem 3rem; } }
    .page-label { font-family: var(--font-en); font-size: 0.75rem; font-weight: 600; letter-spacing: 0.18em; color: var(--accent-cyan); text-transform: uppercase; margin-bottom: 1.5rem; display: block; }
    .page-title { font-family: var(--font-jp); font-size: clamp(2rem, 4vw, 3rem); font-weight: 700; color: var(--text-primary); line-height: 1.3; margin-bottom: 1.5rem; }
    .page-desc { font-size: 1.05rem; color: var(--text-secondary); line-height: 1.9; }

    .section { padding: 6rem 0; }
    .section-inner { max-width: 900px; margin: 0 auto; padding: 0 2rem; }
    .section-label { font-family: var(--font-en); font-size: 0.72rem; font-weight: 700; letter-spacing: 0.18em; color: var(--accent-cyan); text-transform: uppercase; display: block; margin-bottom: 1rem; }
    .section-heading { font-size: clamp(1.6rem, 3vw, 2.2rem); font-weight: 700; color: var(--text-primary); line-height: 1.3; margin-bottom: 2rem; }
    .section-text { font-size: 1rem; color: var(--text-secondary); line-height: 1.9; margin-bottom: 3rem; text-align: center; }
    .alt-bg { background: var(--bg-subtle); }

    /* CHALLENGE CARDS */
    .challenge-list { display: flex; flex-direction: column; gap: 1.5rem; }
    .challenge-card { background: var(--bg-card); border: 1px solid rgba(0,0,0,0.08); border-radius: 12px; padding: 2rem; }
    .challenge-title { font-size: 1.1rem; font-weight: 700; color: var(--text-primary); margin-bottom: 0.75rem; }
    .challenge-desc { font-size: 0.93rem; color: var(--text-secondary); line-height: 1.85; }

    /* STRENGTHS GRID */
    .strengths-grid { display: grid; grid-template-columns: repeat(2, 1fr); gap: 1.5rem; margin-top: 3rem; }
    .strength-card { background: var(--bg-card); border: 1px solid rgba(8,145,178,0.2); border-radius: 12px; padding: 2rem; }
    .strength-title { font-size: 1.05rem; font-weight: 700; color: var(--accent-cyan); margin-bottom: 0.75rem; }
    .strength-desc { font-size: 0.92rem; color: var(--text-secondary); line-height: 1.85; }

    /* TECH TAGS */
    .tech-tags { display: flex; flex-wrap: wrap; gap: 0.75rem; justify-content: center; margin-top: 2rem; }
    .tech-tag { background: var(--bg-card); border: 1px solid rgba(0,0,0,0.1); border-radius: 20px; padding: 0.5rem 1.2rem; font-size: 0.85rem; color: var(--text-secondary); }

    /* PLACEHOLDER */
    .placeholder-box { background: var(--bg-card); border: 1px solid rgba(0,0,0,0.08); border-radius: 12px; padding: 4rem 2rem; text-align: center; color: var(--text-dim); font-size: 0.9rem; }

    /* KEY VALUES */
    .key-values { display: grid; grid-template-columns: repeat(3, 1fr); gap: 1rem; max-width: 760px; margin: 0 auto 4rem; }
    .key-value { display: flex; flex-direction: column; align-items: center; gap: 0.6rem; padding: 1.5rem 1rem; background: #ffffff; border: 1px solid rgba(8,145,178,0.18); border-radius: 12px; text-align: center; }
    .key-value-icon { font-size: 1.5rem; color: var(--accent-cyan); }
    .key-value-text { font-size: 0.9rem; font-weight: 700; color: var(--text-primary); line-height: 1.5; }

    /* ACCORDION */
    .accordion { max-width: 760px; margin: 0 auto; display: flex; flex-direction: column; gap: 0.85rem; }
    .acc-item { background: #ffffff; border: 1px solid rgba(0,0,0,0.08); border-radius: 12px; overflow: hidden; transition: box-shadow 0.3s var(--transition), border-color 0.3s var(--transition); }
    .acc-item[open] { border-color: rgba(8,145,178,0.3); box-shadow: 0 8px 28px rgba(8,145,178,0.08); }
    .acc-summary { display: flex; align-items: center; gap: 1rem; padding: 1.4rem 1.6rem; cursor: pointer; list-style: none; transition: background 0.2s; }
    .acc-summary::-webkit-details-marker { display: none; }
    .acc-summary:hover { background: rgba(8,145,178,0.03); }
    .acc-label { font-family: var(--font-en); font-size: 0.65rem; font-weight: 700; letter-spacing: 0.18em; color: var(--accent-cyan); text-transform: uppercase; min-width: 90px; }
    .acc-title { flex: 1; font-size: 1.02rem; font-weight: 700; color: var(--text-primary); }
    .acc-icon { width: 28px; height: 28px; display: flex; align-items: center; justify-content: center; border-radius: 50%; background: rgba(8,145,178,0.1); color: var(--accent-cyan); font-size: 1.1rem; font-weight: 300; transition: transform 0.3s var(--transition), background 0.3s; line-height: 1; }
    .acc-item[open] .acc-icon { transform: rotate(45deg); background: var(--accent-cyan); color: #ffffff; }
    .acc-body { padding: 0 1.6rem 1.6rem; border-top: 1px solid rgba(0,0,0,0.05); padding-top: 1.4rem; }
    .acc-lead { font-size: 0.92rem; color: var(--text-secondary); line-height: 1.85; margin-bottom: 1.25rem; }
    .acc-list { list-style: none; display: flex; flex-direction: column; gap: 1rem; }
    .acc-list li { display: flex; flex-direction: column; gap: 0.3rem; padding: 0.95rem 1.1rem; background: rgba(8,145,178,0.04); border-left: 3px solid var(--accent-cyan); border-radius: 0 8px 8px 0; }
    .acc-list strong { font-size: 0.92rem; font-weight: 700; color: var(--text-primary); }
    .acc-list span { font-size: 0.85rem; color: var(--text-secondary); line-height: 1.75; }
    .acc-formula { display: flex; align-items: center; justify-content: center; gap: 1rem; flex-wrap: wrap; padding: 1.5rem 1rem; margin: 0 0 1.25rem; background: linear-gradient(135deg, rgba(8,145,178,0.06), rgba(6,182,212,0.04)); border-radius: 10px; font-size: 1.15rem; font-weight: 700; }
    .acc-formula span { background: linear-gradient(135deg, var(--accent-cyan), #06b6d4); -webkit-background-clip: text; -webkit-text-fill-color: transparent; background-clip: text; }
    .acc-formula em { font-style: normal; color: var(--text-dim); font-weight: 300; }
    .acc-foot { font-size: 0.82rem; color: var(--text-dim); line-height: 1.75; margin-top: 1rem; padding-top: 1rem; border-top: 1px dashed rgba(0,0,0,0.08); }

    /* RC PILLARS */
    .rc-pillars { display: grid; grid-template-columns: repeat(3, 1fr); gap: 1.5rem; margin-top: 3.5rem; }
    .rc-pillar { background: #ffffff; border: 1px solid rgba(8,145,178,0.18); border-radius: 14px; padding: 2.25rem 1.75rem; position: relative; overflow: hidden; transition: transform 0.4s var(--transition), box-shadow 0.4s var(--transition); }
    .rc-pillar::before { content: ""; position: absolute; left: 0; top: 0; width: 100%; height: 3px; background: linear-gradient(90deg, var(--accent-cyan), #06b6d4); }
    .rc-pillar:hover { transform: translateY(-4px); box-shadow: 0 18px 48px rgba(8,145,178,0.12); }
    .rc-pillar-num { font-family: var(--font-en); font-size: 0.7rem; font-weight: 700; letter-spacing: 0.2em; color: var(--accent-cyan); display: block; margin-bottom: 1rem; }
    .rc-pillar-title { font-size: 1.1rem; font-weight: 700; color: var(--text-primary); margin-bottom: 0.85rem; line-height: 1.5; }
    .rc-pillar-desc { font-size: 0.9rem; color: var(--text-secondary); line-height: 1.85; }

    /* RC FORMULA */
    .rc-formula { text-align: center; margin: 4rem auto 3rem; }
    .rc-formula-label { font-family: var(--font-en); font-size: 0.7rem; font-weight: 700; letter-spacing: 0.2em; color: var(--text-dim); text-transform: uppercase; margin-bottom: 1.25rem; }
    .rc-formula-text { font-size: clamp(1.4rem, 3vw, 2rem); font-weight: 700; color: var(--text-primary); display: flex; align-items: center; justify-content: center; gap: 1.25rem; flex-wrap: wrap; letter-spacing: 0.05em; }
    .rc-formula-text span { background: linear-gradient(135deg, var(--accent-cyan), #06b6d4); -webkit-background-clip: text; -webkit-text-fill-color: transparent; background-clip: text; }
    .rc-formula-text em { font-style: normal; color: var(--text-dim); font-weight: 300; font-size: 0.85em; }

    /* RC USE CASES */
    .rc-uses { display: grid; grid-template-columns: repeat(3, 1fr); gap: 1rem; margin-top: 1rem; }
    .rc-use { background: #ffffff; border: 1px solid rgba(0,0,0,0.06); border-radius: 12px; padding: 1.75rem 1.5rem; text-align: center; }
    .rc-use-highlight { background: linear-gradient(135deg, rgba(8,145,178,0.06) 0%, rgba(6,182,212,0.04) 100%); border: 1.5px solid rgba(8,145,178,0.3); transform: scale(1.03); }
    .rc-use-title { font-size: 1rem; font-weight: 700; color: var(--text-primary); margin-bottom: 0.65rem; }
    .rc-use-highlight .rc-use-title { color: var(--accent-cyan); }
    .rc-use-desc { font-size: 0.85rem; color: var(--text-secondary); line-height: 1.75; }

    .rc-footnote { font-size: 0.85rem; color: var(--text-dim); text-align: center; margin-top: 2.5rem; line-height: 1.85; max-width: 680px; margin-left: auto; margin-right: auto; }

    /* MOBILE FLOATING CONTACT */
    .mobile-cta-bar { display: none; position: fixed; bottom: 0; left: 0; right: 0; z-index: 99; padding: 0.75rem 1rem calc(0.75rem + env(safe-area-inset-bottom, 0)); background: rgba(255,255,255,0.97); backdrop-filter: blur(12px); border-top: 1px solid rgba(0,0,0,0.08); box-shadow: 0 -4px 24px rgba(0,0,0,0.06); }
    .mobile-cta-btn { display: flex; align-items: center; justify-content: center; gap: 0.5rem; width: 100%; background: linear-gradient(135deg, #0e7490 0%, #0891b2 50%, #06b6d4 100%); color: #ffffff; font-size: 0.95rem; font-weight: 700; letter-spacing: 0.04em; padding: 0.95rem 1.25rem; border-radius: 12px; box-shadow: 0 8px 24px rgba(8,145,178,0.3); }
    .mobile-cta-btn::after { content: "→"; font-size: 1.1em; }

    /* CTA */
    .cta-section { position: relative; background: linear-gradient(135deg, #0e7490 0%, #0891b2 50%, #06b6d4 100%); padding: 7rem 0; text-align: center; overflow: hidden; }
    .cta-section::before { content: ""; position: absolute; inset: 0; background: radial-gradient(circle at 20% 30%, rgba(255,255,255,0.12) 0%, transparent 50%), radial-gradient(circle at 80% 70%, rgba(255,255,255,0.08) 0%, transparent 50%); pointer-events: none; }
    .cta-section::after { content: ""; position: absolute; inset: 0; background-image: linear-gradient(rgba(255,255,255,0.04) 1px, transparent 1px), linear-gradient(90deg, rgba(255,255,255,0.04) 1px, transparent 1px); background-size: 40px 40px; pointer-events: none; }
    .cta-section .section-inner { position: relative; z-index: 1; }
    .cta-heading { font-size: clamp(1.5rem, 2.8vw, 2.2rem); font-weight: 700; color: #ffffff; margin-bottom: 2.5rem; line-height: 1.4; letter-spacing: -0.01em; text-shadow: 0 2px 20px rgba(0,0,0,0.15); }
    .cta-btn { display: inline-flex; align-items: center; gap: 0.5rem; background: #ffffff; color: #0e7490; font-family: var(--font-en); font-weight: 700; font-size: 1rem; letter-spacing: 0.06em; padding: 1.05rem 2.8rem; border-radius: 10px; box-shadow: 0 12px 40px rgba(0,0,0,0.25), 0 4px 12px rgba(0,0,0,0.15); transition: transform 0.25s var(--transition), box-shadow 0.25s var(--transition); }
    .cta-btn:hover { transform: translateY(-3px); box-shadow: 0 18px 50px rgba(0,0,0,0.3), 0 6px 16px rgba(0,0,0,0.18); }
    .cta-note { font-size: 0.82rem; color: rgba(255,255,255,0.75); margin-top: 1.25rem; }

    .footer { padding: 2rem; text-align: center; border-top: 1px solid rgba(0,0,0,0.08); }
    .footer-copy { font-family: var(--font-en); font-size: 0.78rem; color: var(--text-dim); }

    .reveal { opacity: 0; transform: translateY(24px); transition: opacity 0.7s var(--transition), transform 0.7s var(--transition); }
    .reveal.visible { opacity: 1; transform: none; }
    .reveal-delay-1 { transition-delay: 0.1s; }
    .reveal-delay-2 { transition-delay: 0.2s; }
    .reveal-delay-3 { transition-delay: 0.3s; }

    /* RESPONSIVE */
    @media (max-width: 900px) {
      body { padding-bottom: 5rem; }
      .nav { padding: 0.85rem 1rem; }
      .nav-links { display: none; }
      .nav-actions { gap: 0.5rem; }
      .nav-cta { padding: 0.5rem 0.9rem; font-size: 0.72rem; }
      .section { padding: 4rem 1.25rem; }
      .section-inner { padding: 0; max-width: 100%; }
      .section-heading { font-size: clamp(1.5rem, 5vw, 2rem); line-height: 1.4; }
      .page-hero { padding: 9rem 1.25rem 2.5rem; }
      .page-title { font-size: clamp(1.6rem, 6vw, 2.2rem); line-height: 1.4; }
      .page-desc { font-size: 0.92rem; }
      .feature-grid, .equipment-grid, .case-grid { grid-template-columns: 1fr !important; gap: 1rem; }
      .strengths-grid { grid-template-columns: 1fr !important; gap: 1rem; }
      .key-values { grid-template-columns: 1fr; gap: 0.75rem; margin-bottom: 2.5rem; }
      .key-value { flex-direction: row; padding: 1rem 1.25rem; gap: 0.85rem; text-align: left; }
      .key-value-icon { font-size: 1.25rem; }
      .key-value-text { font-size: 0.88rem; }
      .acc-summary { padding: 1.1rem 1.1rem; gap: 0.75rem; flex-wrap: wrap; }
      .acc-label { min-width: auto; font-size: 0.6rem; flex-basis: 100%; }
      .acc-title { font-size: 0.92rem; }
      .acc-body { padding: 1.1rem 1.1rem 1.4rem; }
      .acc-list li { padding: 0.85rem 0.95rem; }
      .acc-formula { font-size: 1rem; padding: 1.1rem 0.75rem; gap: 0.6rem; }
      .rc-pillars { grid-template-columns: 1fr !important; gap: 1rem; margin-top: 2.5rem; }
      .rc-pillar { padding: 1.75rem 1.5rem; }
      .rc-uses { grid-template-columns: 1fr !important; gap: 0.85rem; }
      .rc-use-highlight { transform: none; }
      .rc-formula { margin: 2.5rem auto 2rem; }
      .rc-formula-text { gap: 0.85rem; font-size: 1.4rem; }
      .tech-tags { gap: 0.5rem; }
      .tech-tag { font-size: 0.78rem; padding: 0.45rem 0.85rem; }
      .cta-section { padding: 4.5rem 1.25rem; }
      .cta-heading { font-size: clamp(1.3rem, 5vw, 1.8rem); }
      .cta-btn { padding: 0.95rem 2rem; font-size: 0.92rem; }
      .mobile-cta-bar { display: block; }
    }
  </style>
</head>

<body>

  <!-- NAV -->
  <nav class="nav" id="nav">
    <a href="/" class="nav-logo">ZETA<span>X</span></a>
    <ul class="nav-links">
      <li><a href="/">Home</a></li>
      <li><a href="/#solutions">Solutions</a></li>
      <li><a href="/#company">Company</a></li>
      <li><a href="/#news">News</a></li>
    </ul>
    <div class="nav-actions">
      <a href="/#contact" class="nav-cta" data-ja="お問い合わせ" data-en="Contact">お問い合わせ</a>
      <div class="lang-toggle">
        <button class="lang-btn active" data-lang="ja" onclick="switchLang('ja')">JP</button>
        <button class="lang-btn" data-lang="en" onclick="switchLang('en')">EN</button>
      </div>
    </div>
  </nav>

  <!-- PAGE HERO -->
  <div class="page-hero reveal">
    <span class="page-label">Anomaly Detection</span>
    <h1 class="page-title" data-ja="その異常、人が気づく前に検知する。" data-en="Detect anomalies before humans notice them.">その異常、人が気づく前に検知する。</h1>
    <p class="page-desc" data-ja="製造現場の「いつもと何か違う」を、リアルタイムで捉える。東大・東工大の研究から生まれた異常検知技術を、あなたの工場に実装します。" data-en="Capture the subtle shifts on your manufacturing floor in real time. We implement anomaly detection technology born from Tokyo and Tokyo Tech research.">製造現場の「いつもと何か違う」を、リアルタイムで捉える。東大・東工大の研究から生まれた異常検知技術を、あなたの工場に実装します。</p>
  </div>

  <!-- KEY VALUES -->
  <section class="section">
    <div class="section-inner">
      <div class="key-values reveal">
        <div class="key-value">
          <span class="key-value-icon">⚡</span>
          <p class="key-value-text" data-ja="GPU不要・クラウド不要" data-en="No GPU. No cloud.">GPU不要・クラウド不要</p>
        </div>
        <div class="key-value">
          <span class="key-value-icon">⚙</span>
          <p class="key-value-text" data-ja="導入初日から検知開始" data-en="Detection from day one.">導入初日から検知開始</p>
        </div>
        <div class="key-value">
          <span class="key-value-icon">◎</span>
          <p class="key-value-text" data-ja="東大・東工大発の独自技術" data-en="Tokyo / Tokyo Tech tech.">東大・東工大発の独自技術</p>
        </div>
      </div>

      <!-- ACCORDION DETAILS -->
      <div class="accordion reveal reveal-delay-1">
        <details class="acc-item">
          <summary class="acc-summary">
            <span class="acc-label">Challenges</span>
            <span class="acc-title" data-ja="こんな課題、ありませんか？" data-en="Sound familiar?">こんな課題、ありませんか？</span>
            <span class="acc-icon" aria-hidden="true">+</span>
          </summary>
          <div class="acc-body">
            <ul class="acc-list">
              <li><strong data-ja="壊れてから直す、の繰り返し。" data-en="Fix it after it breaks.">壊れてから直す、の繰り返し。</strong><span data-ja="計画外のダウンタイムが利益を削り、修理コストが積み上がる。" data-en="Unplanned downtime erodes profit. Repair costs pile up.">計画外のダウンタイムが利益を削り、修理コストが積み上がる。</span></li>
              <li><strong data-ja="AI検知を入れたが、使われていない。" data-en="AI detection installed, but unused.">AI検知を入れたが、使われていない。</strong><span data-ja="クラウド型は誤検知が多く、現場が信用しない。結局ベテラン巡回に戻っている。" data-en="Cloud-based had too many false alarms. Back to veteran inspections.">クラウド型は誤検知が多く、現場が信用しない。結局ベテラン巡回に戻っている。</span></li>
              <li><strong data-ja="ベテランの「勘」が引き継げない。" data-en="Veteran intuition can't be passed on.">ベテランの「勘」が引き継げない。</strong><span data-ja="熟練者の感覚は言語化できず、人が変われば検知力も消える。" data-en="Experts sense subtle shifts but can't explain them. The skill leaves with them.">熟練者の感覚は言語化できず、人が変われば検知力も消える。</span></li>
            </ul>
          </div>
        </details>

        <details class="acc-item">
          <summary class="acc-summary">
            <span class="acc-label">Approach</span>
            <span class="acc-title" data-ja="現場専用の検知モデルを作る。" data-en="A model built for your site.">現場専用の検知モデルを作る。</span>
            <span class="acc-icon" aria-hidden="true">+</span>
          </summary>
          <div class="acc-body">
            <p class="acc-lead" data-ja="同じモーターでも、設置環境・稼働パターン・経年度合いで「正常」の基準は違う。汎用モデルでは精度が出ない。だから現場の正常稼働データから専用モデルを構築します。" data-en="Even the same motor has different normal baselines depending on installation, usage, and age. Generic models fail. We build from your normal operating data.">同じモーターでも、設置環境・稼働パターン・経年度合いで「正常」の基準は違う。汎用モデルでは精度が出ない。だから現場の正常稼働データから専用モデルを構築します。</p>
            <ul class="acc-list">
              <li><strong data-ja="エッジ完結" data-en="Edge-only">エッジ完結</strong><span data-ja="製造データが外に出ず、ネットワークなしの現場にも導入可能。" data-en="Data never leaves your facility. Works offline.">製造データが外に出ず、ネットワークなしの現場にも導入可能。</span></li>
              <li><strong data-ja="少データ学習" data-en="Small-data learning">少データ学習</strong><span data-ja="正常データだけで短時間にモデル構築。大量の学習データは不要。" data-en="Build from normal data alone. No massive datasets needed.">正常データだけで短時間にモデル構築。大量の学習データは不要。</span></li>
              <li><strong data-ja="自律追従" data-en="Self-adapting">自律追従</strong><span data-ja="季節変動・経年劣化・条件変更にも常時再学習で対応。" data-en="Continuously relearns through seasonal shifts and aging.">季節変動・経年劣化・条件変更にも常時再学習で対応。</span></li>
              <li><strong data-ja="理由が分かる" data-en="Explainable">理由が分かる</strong><span data-ja="どのセンサー値が異常を示したか明確に表示し、現場が動ける。" data-en="Shows exactly which sensors triggered the alert.">どのセンサー値が異常を示したか明確に表示し、現場が動ける。</span></li>
            </ul>
          </div>
        </details>

        <details class="acc-item">
          <summary class="acc-summary">
            <span class="acc-label">Core Tech</span>
            <span class="acc-title" data-ja="独自アルゴリズムの特長" data-en="Our Proprietary Algorithm">独自アルゴリズムの特長</span>
            <span class="acc-icon" aria-hidden="true">+</span>
          </summary>
          <div class="acc-body">
            <p class="acc-lead" data-ja="東京大学・東京科学大学の研究室との共創から生まれた独自アルゴリズムを中核技術としています。従来のディープラーニングとは全く異なる原理で、時系列データの特徴抽出に特化。少ないデータ量・低消費電力・エッジ完結という、製造現場の要件を同時に満たします。" data-en="Our core technology is a proprietary algorithm developed with the University of Tokyo and Institute of Science Tokyo research labs. Unlike conventional deep learning, it specializes in time-series feature extraction—simultaneously solving the three manufacturing-floor constraints of small data, low power, and edge-only operation.">東京大学・東京科学大学の研究室との共創から生まれた独自アルゴリズムを中核技術としています。従来のディープラーニングとは全く異なる原理で、時系列データの特徴抽出に特化。少ないデータ量・低消費電力・エッジ完結という、製造現場の要件を同時に満たします。</p>
            <div class="acc-formula"><span>非線形</span><em>×</em><span data-ja="記憶" data-en="Memory">記憶</span><em>×</em><span data-ja="時系列" data-en="Time-series">時系列</span></div>
            <ul class="acc-list">
              <li><strong data-ja="少ないデータで動く" data-en="Minimal data">少ないデータで動く</strong><span data-ja="数年分のデータが揃わない現場でも初日から検知可能。" data-en="Detection starts day one—no historical data required.">数年分のデータが揃わない現場でも初日から検知可能。</span></li>
              <li><strong data-ja="圧倒的な低消費電力" data-en="Ultra-low power">圧倒的な低消費電力</strong><span data-ja="学習コストを極小化する設計により、ほぼ電力を消費せず動作。発熱問題から解放。" data-en="A design that minimizes training cost—almost zero power draw, no GPU heat.">学習コストを極小化する設計により、ほぼ電力を消費せず動作。発熱問題から解放。</span></li>
              <li><strong data-ja="時系列データに桁違いに強い" data-en="Built for time-series">時系列データに桁違いに強い</strong><span data-ja="振動・電流・温度・音響など、波形データの特徴抽出が本領。" data-en="Vibration, current, temperature, acoustics—native strength.">振動・電流・温度・音響など、波形データの特徴抽出が本領。</span></li>
            </ul>
            <p class="acc-foot" data-ja="弱点（長期記憶・大規模画像認識）は既存AIと組み合わせることで補完し、独自アルゴリズムの強みを最大化します。" data-en="We complement the algorithm's weaknesses (long-term memory, large-scale image recognition) with conventional AI to maximize its strengths.">弱点（長期記憶・大規模画像認識）は既存AIと組み合わせることで補完し、独自アルゴリズムの強みを最大化します。</p>
          </div>
        </details>

        <details class="acc-item">
          <summary class="acc-summary">
            <span class="acc-label">Coverage</span>
            <span class="acc-title" data-ja="対応できる設備領域" data-en="Equipment we handle">対応できる設備領域</span>
            <span class="acc-icon" aria-hidden="true">+</span>
          </summary>
          <div class="acc-body">
            <div class="tech-tags">
              <span class="tech-tag" data-ja="回転機器（モーター・ポンプ・コンプレッサー）" data-en="Rotating Equipment">回転機器（モーター・ポンプ・コンプレッサー）</span>
              <span class="tech-tag" data-ja="配管・バルブ" data-en="Piping &amp; Valves">配管・バルブ</span>
              <span class="tech-tag" data-ja="コンベヤ・搬送機器" data-en="Conveyors">コンベヤ・搬送機器</span>
              <span class="tech-tag" data-ja="空調・環境設備" data-en="HVAC">空調・環境設備</span>
              <span class="tech-tag" data-ja="集塵機・排気設備" data-en="Exhaust">集塵機・排気設備</span>
              <span class="tech-tag" data-ja="外観検査" data-en="Visual Inspection">外観検査</span>
            </div>
          </div>
        </details>

        <details class="acc-item">
          <summary class="acc-summary">
            <span class="acc-label">Track Record</span>
            <span class="acc-title" data-ja="プライム上場企業から町工場まで" data-en="From listed firms to small workshops">プライム上場企業から町工場まで</span>
            <span class="acc-icon" aria-hidden="true">+</span>
          </summary>
          <div class="acc-body">
            <p class="acc-lead" data-ja="東証プライム上場企業との実証実験を実施中。複数の町工場への導入が決定済み。大規模製造拠点から中小規模の工場まで、幅広い現場で実績があります。" data-en="Running PoC with a TSE Prime-listed company. Multiple small factory deployments confirmed. From large sites to small operations.">東証プライム上場企業との実証実験を実施中。複数の町工場への導入が決定済み。大規模製造拠点から中小規模の工場まで、幅広い現場で実績があります。</p>
          </div>
        </details>
      </div>
    </div>
  </section>

  <!-- CTA -->
  <div class="cta-section">
    <div class="section-inner">
      <h2 class="cta-heading reveal" data-ja="あなたの設備の「正常」を、科学的に定義する。" data-en="We define your equipment's normal scientifically.">あなたの設備の「正常」を、科学的に定義する。</h2>
      <a href="/#contact" class="cta-btn reveal reveal-delay-1" data-ja="相談してみる" data-en="Let's talk">相談してみる</a>
      <p class="cta-note reveal reveal-delay-2" data-ja="担当者からメールでご連絡させていただきます。" data-en="Our team will reach out via email.">担当者からメールでご連絡させていただきます。</p>
    </div>
  </div>

  <!-- MOBILE FLOATING CTA -->
  <div class="mobile-cta-bar">
    <a href="/#contact" class="mobile-cta-btn" data-ja="20分の無料相談を予約" data-en="Book a 20-min consultation">20分の無料相談を予約</a>
  </div>

  <!-- FOOTER -->
  <footer class="footer">
    <p class="footer-copy">&copy; 2026 ZetaX Inc. All Rights Reserved.</p>
  </footer>

  <script is:inline>
    const nav = document.getElementById('nav');
    window.addEventListener('scroll', () => { nav.classList.toggle('scrolled', window.scrollY > 50); });

    const reveals = document.querySelectorAll('.reveal');
    const obs = new IntersectionObserver((entries) => {
      entries.forEach(e => { if (e.isIntersecting) e.target.classList.add('visible'); });
    }, { threshold: 0.12, rootMargin: '0px 0px -40px 0px' });
    reveals.forEach(r => obs.observe(r));
    setTimeout(() => reveals.forEach(r => { const rect = r.getBoundingClientRect(); if (rect.top < window.innerHeight) r.classList.add('visible'); }), 100);

    function switchLang(lang) {
      document.documentElement.dataset.lang = lang;
      document.querySelectorAll('.lang-btn').forEach(b => b.classList.toggle('active', b.dataset.lang === lang));
      document.querySelectorAll('[data-ja]').forEach(el => {
        const txt = el.dataset[lang];
        if (txt !== undefined) el.innerHTML = txt;
      });
    }
  </script>
</body>
</html>
